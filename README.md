# File-Sorter

This is a file sorter for python. It was made by me (JL Studios) and it is for sorting folders and files in a specific directory

## Installation

1. To install this program first go to https://www.python.org/downloads/release/python-3127/

2. Download the installer that supports your Operating System

3. Open and follow the instructions from the python installer

4. close the installer and run the **main.py** file in the source folder

## How to use

- The button **Make separate folder in dir** toggles if the sorted files will get sorted in the selected path / directory or if they go into a new folder with the name **Sorted files**

- If the button **Sort all dirs in this directory separately** is toggled then it will separately sort all the folders in the selected path / directory. If not toggled it will sort all the files / folders in the currently selected path / directory

- The **Select directory** opens a folder selection dialog to select the path / directory to sort

- The **SORT** button sorts the selected path / directory

## Future plans

- make the program into a **.exe**, **.app** and other file executables for native app support on different operating systems
- make a node based system to completely customize the way the files get sorted
- make it more understandable for non developers
